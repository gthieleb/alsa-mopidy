---
- hosts: audio-stations 
  become: true
  gather_facts: true
  vars:
      mopidy:
          playlists_dir: "/var/lib/mopidy/playlists"
          media_dir: "/media/Music"
      optimise:
          internal_audio: true
          update_firmware: false
          dac: false
      user: mopidy
      httpd: 
        hostname: "0.0.0.0"
      mpd: 
        hostname: "127.0.0.1"
      services:
        - mopidy
      main:
        apt:
          - ntp
          - mopidy
          - python-pip
      extensions:
        apt:
          - mopidy-podcast
          - mopidy-somafm
          - mopidy-tunein
          - mopidy-youtube
          - mopidy-local-sqlite
          - mopidy-alsamixer
      webclients:
        pip:
          - Mopidy-MusicBox-Webclient

  handlers:
    - include: roles/setup/handlers/main.yml 
            
  roles:
    - setup
    - setup-extensions
    - setup-webclients
    - soundcard-settings

